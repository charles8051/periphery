// Wall-clock APIs banned in tests. ADR-0089 has the reasoning.
//
// Not banned, on purpose: Task.WaitAsync(TimeSpan), new CancellationTokenSource(TimeSpan) and
// CancelAfter. As safety nets that bound a failure they are correct and wanted. A timeout used as
// the assertion itself is still disallowed, but the analyzer cannot tell the two apart, so that
// boundary is held by review. See D5 in ADR-0089.
//
// The list matches frame-flow's tests/BannedSymbols.txt (its ADR-0072), so the two repositories ban
// the same things.
M:System.Threading.Thread.Sleep(System.Int32);Sleeping is a bet on scheduler latency. Wait on the thing you expect instead, or advance a FakeTimeProvider. See ADR-0089.
M:System.Threading.Thread.Sleep(System.TimeSpan);Sleeping is a bet on scheduler latency. Wait on the thing you expect instead, or advance a FakeTimeProvider. See ADR-0089.
M:System.Threading.Tasks.Task.Delay(System.Int32);A delay in a test guesses how long something takes. Await a signal from the code under test, or use Task.Delay(TimeSpan, TimeProvider) with a FakeTimeProvider. See ADR-0089.
M:System.Threading.Tasks.Task.Delay(System.TimeSpan);A delay in a test guesses how long something takes. Await a signal from the code under test, or use Task.Delay(TimeSpan, TimeProvider) with a FakeTimeProvider. See ADR-0089.
M:System.Threading.Tasks.Task.Delay(System.Int32,System.Threading.CancellationToken);A delay in a test guesses how long something takes. Await a signal from the code under test, or use Task.Delay(TimeSpan, TimeProvider, CancellationToken) with a FakeTimeProvider. See ADR-0089.
M:System.Threading.Tasks.Task.Delay(System.TimeSpan,System.Threading.CancellationToken);A delay in a test guesses how long something takes. Await a signal from the code under test, or use Task.Delay(TimeSpan, TimeProvider, CancellationToken) with a FakeTimeProvider. See ADR-0089.
P:System.DateTime.Now;Reading the wall clock makes a test depend on when it runs. Inject a TimeProvider and use FakeTimeProvider. See ADR-0089.
P:System.DateTime.UtcNow;Reading the wall clock makes a test depend on when it runs. Inject a TimeProvider and use FakeTimeProvider. See ADR-0089.
P:System.DateTimeOffset.Now;Reading the wall clock makes a test depend on when it runs. Inject a TimeProvider and use FakeTimeProvider. See ADR-0089.
P:System.DateTimeOffset.UtcNow;Reading the wall clock makes a test depend on when it runs. Inject a TimeProvider and use FakeTimeProvider. See ADR-0089.
M:System.Diagnostics.Stopwatch.#ctor;Measuring elapsed time in a test asserts how fast the machine is. Assert the behaviour instead; real measurements belong behind an opt-in. See ADR-0089.
M:System.Diagnostics.Stopwatch.StartNew;Measuring elapsed time in a test asserts how fast the machine is. Assert the behaviour instead; real measurements belong behind an opt-in. See ADR-0089.
M:System.Diagnostics.Stopwatch.GetTimestamp;Measuring elapsed time in a test asserts how fast the machine is. Assert the behaviour instead; real measurements belong behind an opt-in. See ADR-0089.
M:System.Diagnostics.Stopwatch.GetElapsedTime(System.Int64);Measuring elapsed time in a test asserts how fast the machine is. Assert the behaviour instead; real measurements belong behind an opt-in. See ADR-0089.
M:System.Diagnostics.Stopwatch.GetElapsedTime(System.Int64,System.Int64);Measuring elapsed time in a test asserts how fast the machine is. Assert the behaviour instead; real measurements belong behind an opt-in. See ADR-0089.
P:System.Environment.TickCount;The tick count is the machine's clock under another name. Inject a TimeProvider and use FakeTimeProvider. See ADR-0089.
P:System.Environment.TickCount64;The tick count is the machine's clock under another name. Inject a TimeProvider and use FakeTimeProvider. See ADR-0089.
M:System.Threading.Timer.#ctor(System.Threading.TimerCallback);A timer constructed directly fires on the machine's clock. Create it with TimeProvider.CreateTimer and drive it with a FakeTimeProvider. See ADR-0089.
M:System.Threading.Timer.#ctor(System.Threading.TimerCallback,System.Object,System.Int32,System.Int32);A timer constructed directly fires on the machine's clock. Create it with TimeProvider.CreateTimer and drive it with a FakeTimeProvider. See ADR-0089.
M:System.Threading.Timer.#ctor(System.Threading.TimerCallback,System.Object,System.Int64,System.Int64);A timer constructed directly fires on the machine's clock. Create it with TimeProvider.CreateTimer and drive it with a FakeTimeProvider. See ADR-0089.
M:System.Threading.Timer.#ctor(System.Threading.TimerCallback,System.Object,System.TimeSpan,System.TimeSpan);A timer constructed directly fires on the machine's clock. Create it with TimeProvider.CreateTimer and drive it with a FakeTimeProvider. See ADR-0089.
M:System.Threading.Timer.#ctor(System.Threading.TimerCallback,System.Object,System.UInt32,System.UInt32);A timer constructed directly fires on the machine's clock. Create it with TimeProvider.CreateTimer and drive it with a FakeTimeProvider. See ADR-0089.
M:System.Timers.Timer.#ctor;A timer constructed directly fires on the machine's clock. Create it with TimeProvider.CreateTimer and drive it with a FakeTimeProvider. See ADR-0089.
M:System.Timers.Timer.#ctor(System.Double);A timer constructed directly fires on the machine's clock. Create it with TimeProvider.CreateTimer and drive it with a FakeTimeProvider. See ADR-0089.
M:System.Timers.Timer.#ctor(System.TimeSpan);A timer constructed directly fires on the machine's clock. Create it with TimeProvider.CreateTimer and drive it with a FakeTimeProvider. See ADR-0089.
M:System.Threading.PeriodicTimer.#ctor(System.TimeSpan);A PeriodicTimer without a TimeProvider ticks on the machine's clock. Use new PeriodicTimer(TimeSpan, TimeProvider) with a FakeTimeProvider. See ADR-0089.
